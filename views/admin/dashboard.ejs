<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin – Poptávky | VyššíCena</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-900">
  <!-- Header (mobil přehledně) -->
  <header class="bg-white border-b">
    <div class="mx-auto max-w-6xl px-4 py-2 flex items-center justify-between gap-2">
      <div class="flex items-center gap-2 min-w-0">
        <img src="/logo.svg" alt="VyššíCena" class="h-7 w-auto shrink-0">
      </div>
      <nav class="flex items-center gap-2 md:gap-3 shrink-0">
        <a href="/" class="text-xs md:text-sm text-slate-600 hover:text-slate-900">Veřejný web</a>
        <a href="/admin/logout" class="text-xs md:text-sm px-2 md:px-3 py-1.5 rounded-lg border hover:bg-slate-50">Odhlásit</a>
      </nav>
    </div>
  </header>

  <main class="mx-auto max-w-6xl px-4 py-6 md:py-8">
    <div class="mb-4 md:mb-6 flex items-center justify-between gap-3">
      <h1 class="text-xl md:text-2xl font-bold">Poptávky</h1>
      <div class="text-xs md:text-sm text-slate-500"><%= leads?.length || 0 %> záznamů</div>
    </div>

<!-- MOTIVAČNÍ BANNER (kompaktní verze) -->
<div class="mb-6 rounded-xl p-3 bg-gradient-to-r from-slate-900/90 via-slate-800/90 to-slate-700/90 text-white shadow-sm border border-slate-700">
  <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-2">
    <div>
      <h2 class="text-base font-semibold tracking-tight flex items-center gap-1.5">
        📈🚀 <span>Rostislav Kandel — On his way to the TOP</span>
      </h2>
      <p class="text-slate-300 mt-1 text-sm italic">
        „Někdo dnes sedí ve stínu, protože někdo před ním zasadil strom.“ 🌳
      </p>
    </div>
    <div class="hidden md:block opacity-80">
        <path stroke-linecap="round" stroke-linejoin="round"
              d="M12 6v6l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
    </div>
  </div>
</div>


    <!-- Mobil: horizontální scroll tabulky -->
    <div class="bg-white border rounded-2xl overflow-hidden">
      <div class="overflow-x-auto"> <!-- 👈 tohle vrací vodorovný scroll na mobilu -->
        <table class="min-w-[820px] md:min-w-full text-sm">
          <thead class="bg-slate-50 border-b text-slate-600">
            <tr>
              <th class="text-left py-3 px-3">ID</th>
              <th class="text-left py-3 px-3">Jméno</th>
              <th class="text-left py-3 px-3">Typ</th>
              <th class="text-left py-3 px-3">Město</th>
              <th class="text-left py-3 px-3">PSČ</th>
              <th class="text-left py-3 px-3">Plocha</th>
              <th class="text-left py-3 px-3">Vytvořeno</th>
              <th class="text-left py-3 px-3">Stav</th>
              <th class="text-left py-3 px-3">Balkon</th>
              <th class="text-right py-3 px-3">Zavoláno</th>
            </tr>
          </thead>

          <tbody>
            <% if (!leads || leads.length === 0) { %>
              <tr>
                <td colspan="8" class="py-10 text-center text-slate-500">
                  Zatím žádné poptávky.
                </td>
              </tr>
            <% } %>

            <% leads.forEach(lead => { %>
              <!-- Klikací řádek (kromě poslední buňky se switch/checkboxem) -->
              <tr
                data-href="/admin/leads/<%= lead.id %>"
                class="border-b last:border-0 hover:bg-slate-50 transition cursor-pointer"
              >
                <td class="py-3 px-3 font-medium text-slate-800"><%= lead.id %></td>
                <td class="py-3 px-3"><%= lead.first_name %> <%= lead.last_name %></td>
                <td class="py-3 px-3 uppercase"><%= lead.type %></td>
                <td class="py-3 px-3"><%= lead.city %></td>
                <td class="py-3 px-3"><%= lead.psc %></td>
                <td class="py-3 px-3">
                  <%= lead.area %> m²
                  <% if (lead.layout) { %>
                    , <span class="text-slate-500"><%= lead.layout %></span>
                  <% } %>
                </td>
                <td class="py-3 px-3 text-slate-500">
                  <%= new Date(lead.created_at).toLocaleString('cs-CZ') %>
                </td>
                <td class="py-3 px-3"><%= lead.condition || '—' %></td>
                <td class="py-3 px-3"><% if (lead.type === 'byt' && lead.balcony) { %><%= lead.balcony %><% } else { %>neobsahuje<% } %></td>
 

                <!-- Pravá strana: kulatý zelený checkbox; neklikatelná oblast řádku -->
                <td class="py-3 px-3">
                  <form
                    action="/admin/leads/<%= lead.id %>/contacted"
                    method="POST"
                    class="w-full flex items-center justify-end no-rowlink"
                  >
                    <input
                      type="checkbox"
                      aria-label="Zavoláno"
                      onchange="this.form.submit()"
                      <%= lead.contacted ? 'checked' : '' %>
                      class="h-6 w-6 appearance-none rounded-full border-2 border-slate-300
                             cursor-pointer transition-all duration-200
                             checked:bg-green-500 checked:border-green-500
                             hover:border-green-400 focus:ring-2 focus:ring-green-300"
                    />
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </main>

  <script>
    // Klikací řádky → přechod na detail; ignoruj klik uvnitř .no-rowlink (checkbox/form)
    document.querySelectorAll('tr[data-href]').forEach(row => {
      row.addEventListener('click', (e) => {
        if (e.target.closest('.no-rowlink')) return;
        const href = row.getAttribute('data-href');
        if (href) window.location.href = href;
      });
    });
  </script>
</body>
</html>
