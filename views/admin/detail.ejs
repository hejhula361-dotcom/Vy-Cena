<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Detail poptávky #<%= lead.id %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50">
  <header class="bg-white border-b">
    <div class="mx-auto max-w-4xl px-4 py-4 flex items-center justify-between">
      <a href="/admin" class="text-sm underline">← Zpět na přehled</a>
      <a href="/admin/logout" class="text-sm text-slate-600 hover:text-slate-900">Odhlásit</a>
    </div>
  </header>

  <main class="mx-auto max-w-4xl px-4 py-6">
    <div class="bg-white border rounded-2xl p-6">
      <h1 class="text-xl font-semibold mb-4">Poptávka #<%= lead.id %></h1>
      <div class="grid md:grid-cols-2 gap-6 text-sm">
        <div class="space-y-1.5">
          <h2 class="font-medium mb-2">Nemovitost</h2>
          <p><span class="text-slate-500">Město:</span> <%= lead.city %></p>
          <p><span class="text-slate-500">PSČ:</span> <%= lead.psc %></p>
          <p><span class="text-slate-500">Typ:</span> <%= lead.type %></p>
          <p><span class="text-slate-500">Užitná plocha:</span> <%= lead.area %> m²</p>
          <p><span class="text-slate-500">Dispozice:</span> <%= lead.layout %></p>
        </div>
        <div class="space-y-1.5">
          <h2 class="font-medium mb-2">Kontakt</h2>
          <p><span class="text-slate-500">Jméno:</span> <%= lead.first_name %> <%= lead.last_name %></p>
          <p><span class="text-slate-500">E-mail:</span> <a class="underline" href="mailto:<%= lead.email %>"><%= lead.email %></a></p>
          <p><span class="text-slate-500">Telefon:</span> <a class="underline" href="tel:<%= lead.phone %>"><%= lead.phone %></a></p>
          <p><span class="text-slate-500">Vytvořeno:</span> <%= new Date(lead.created_at).toLocaleString('cs-CZ') %></p>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
